# LEVEL 5 — THE BUSTER CALL TIMELINE RECOVERY

### Repository: Terminal-Voyage-User-Edition

### Target Path: -/Terminal-Voyage-User-Edition/GrandLine/Enies_Lobby/.cp9_secure_vault/

### Objective :

The objectvie of the task is recover a deleted challenge file . Get into the branch , later find a ile having a python script, run the script and fill the required needed key to get the way to get into the next level .

## My approach :

First using `git branch -a` then switch into the alternate_timeline branch  using `git checkout` , after you are in the branch run the command `git log --oneline` this perticalar command displays the git commit history by order in a single line. Then run the command `git checkout d4e7bf5` to Get into the branch Level 5 : Vault Sealed . After you are into the desired branch get into GrandLine and Enies_Lobby, after later find the hidden folder i.e `.cp9_secure_vault` and then get into the folder , after u get into the folder you would find a script `poneglyph.py` , now run the script. 

After you run the script you would end up with the `Enter code:` input and just put the fargments obtained as the input and volaaa 

Sucesfully completed the level with a link directing toward a git repo containing the details for level-6 .

## Prize:
[https://github.com/rogueone-x/Laugh-Tale-Merge-War]
