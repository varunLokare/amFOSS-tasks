### Level 6 – The Great Merge War at Laugh Tale

## Repository: Laugh-Tale-Merge-War

## Target Path: ~/Laugh-Tale-Merge-War/

## Objective :

The main objective of the level was to resolve the git merge conflicts between two distinct branches to find the final key to successfully end the Task.

## My Approach :

First check all the branches in the and then merge the branch using the command `git merge origin/pirate_king_path` so after u the command is been runned the main branch and the specified branch merges . Now get into "treasure" there you would find two text files i.e `(key_part_1.txt and key_part_2.txt)` now got through each of the text files one by one and then manually reomve the conflicts present in the txt files . After uve done correcting the conflict stage both the files using the ` git add ` command then then commit the changes. After the commit is completed check the .txt files once again for the framgented keys ( password ) and note them. After that run the script `./victory.sh` and then they drop the obtained password as the input for it 

AND NOW YOU HAVE SUCESSFULLY COMPLETED THE TASK......

## Pirate King's Password: TheGrandLineRemembers

